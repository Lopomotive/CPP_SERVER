//Header file that checks if config file is valid 
#include <string>
#include <iostream> 
#include <algorithm> 
#include <filesystem>
#include <string> 
#include <fstream> //Is that what it is called? 


bool Conf_check(const *string fn){
  fo
    if(fn.substr(fn.find_last_of(".") + 1) == conf){
      return true;
    }
    else{

    }
} 

using namespace std;
namespace fs = std::filesystem;
namespace file_exist{
  bool file(const string& filePath){
    return fs::exists(filePath);
  }
}

namespace folder_exist
class CheckConfig{
  public:
    const string folder_name = "Config/";
    if(fs::is_directory(folder_name) && file_exist) 
      
  protected:
    unsigned char* ChekConfigFile(); 
}
